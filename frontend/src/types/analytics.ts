// Define types for analytics data used in the Boston Startup Tracker frontend

// FundingTrend represents a data point for funding trends over time
export interface FundingTrend {
    year: number;
    totalFunding: number;
}

// Add more analytics-related types here as needed